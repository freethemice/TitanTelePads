name: ${project.artifactId}
version: '${project.version}'
main: ${project.groupId}.${project.artifactId}
api-version: ${minecraft.api-version}
authors: [ freethemice ]
description: ${project.description}
website: ${project.url}
depend:
  - TitanBoxLibs
commands:
  telepads:
    description: telepads
    usage: /<command>
  telepad:
    description: telepad
    usage: /<command>
  tep:
    description: tep
    usage: /<command>
